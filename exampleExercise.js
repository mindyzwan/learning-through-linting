var numbers = [1, 2, 3, 4, 5];

let increasedByOne = new Array();
for (let i = 0; i < numbers.length; i++) {
  increasedByOne.push(numbers[i] + 1);
}

console.log('Huzzah!');
console.log('We increased each number by one!');
